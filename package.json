{
  "name": "xenvia-backend",
  "version": "0.1.0",
  "description": "Xenvia data-sync backend: scrapes official/PriceOye/GSM Arena/WhatMobile on a schedule, normalizes into SQLite, and serves a REST API the web catalogue (and, later, an Android app) can read from.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "init-db": "node scripts/init-db.js",
    "sync-now": "node scripts/sync-now.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "better-sqlite3": "^11.3.0",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=18"
  }
}
